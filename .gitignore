# ── credentials — never commit ────────────────────────────────────────────
.env
*.key
**/secrets*

# ── the corpus: large, scraped, and not ours to redistribute ──────────────
# Supply it locally or via COMPASS_CORPUS_CSV. See DATA_REQUIREMENTS.md.
data/acceptance_rejected_college_data_verified.csv
data/*.sqlite
data/ipeds*

# ── generated output ──────────────────────────────────────────────────────
out/
*.pdf
*.state.json
*.plan.json

# ── python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
